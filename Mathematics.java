/**
 * 
 * @author Adam Banks-Beard
 * 
 * An interface for various math operations
 *
 */
public interface Mathematics {
	float addition(float... add);
	float subtraction(float... sub);
	float multiplication(float... mult);
	float division(float... div);
	float findMax(float... max);
	float findMin(float... min);
	float getAverage(float... avg);
	float getMedian(float... med);
}
